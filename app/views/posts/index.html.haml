- @posts.each do |post|
  %h2= link_to post.title, post
  = link_to (image_tag post.image.url(:small))
  %p
    = post.comments.count
    Comments
  %p
    = post.get_likes.size
    Likes
%p= link_to("New Inspiration", new_post_url)


